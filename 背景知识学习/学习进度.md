# 学习进度（滴水算法项目）

> **给新会话的引导**：请先完整读本文件，了解学习计划与当前进度，从「当前进度」标记处继续辅导，
> 不要重复已完成的讲解。本文件由学习辅助对话创建于 2026-09-05。
> 配套材料：本目录 `01-Python基础.md` ~ `06-背景知识.md` 六课讲义 + `07-MATLAB移植心得.md`
> （MATLAB 侧，配 `../MATLAB/` 移植代码）+ `08-指标A实验解读.md`（第 3 步实验记录）+
> `09-怎么读test_patent.md`（第 4 步导读：Q 是什么、断言骨架、十一条图对照）+
> `10-怎么读segment.py.md`（编排层读法：三步法 / δ 带 / tie-break / 裁剪边界）+
> `11-如何读代码.md`（通用读码五步法，以 `improved_droplet.py` 为实例）+
> `../专利滴水算法实现/` 项目代码。

---

## 学习路径总览

| 阶段 | 内容 | 状态 |
|---|---|---|
| 一 | 基础知识：六课讲义全部读完 | ✅ 已完成 |
| 二 | 动手清单：跑 → 玩 → 改 → 读（见下） | ✅ 第 1~4 步完成；第 5 步语法补充随时可查；可选加餐：补 3 个 s 分支断言 |
| 三 | 进阶（清单完成后自选）：MATLAB 移植 / 真实粘连谱字切分改进 | MATLAB 移植 ✅（2026-09-07 交付 `../MATLAB/` 并经本机验证）；真实粘连改进 ✅ **已收口**——三轮研究后判定"复现论文图3-23 的 0.524"这一目标本身不成立（见文末"阶段三·第三轮"），本实现自身一致性验证全绿 |

---

## 动手清单（按顺序做，每步答完自查题再进下一步）

### 第 1 步：跑 —— 亲眼看到红/绿两刀（约 5 分钟）

```bash
cd "D:/Study/算法学习/专利滴水算法实现"
python demo_segment.py
```

打开 `output/overlay_无粘连.png`。

- **该看到**：一张仿古琴谱字图，红线大致水平横切（上下分开），绿线竖直只在上半区（左右分开）。
- **自查**：红线为什么在图像中部？绿线为什么没画到底部？
  （提示：第二刀从上区上缘出发、碰到红线即停。）

**我的记录**（2026-09-06 完成，答案由辅导对话填入）：

> 运行 `python demo_segment.py` 成功。打开 `output/overlay_无粘连.png`：
> 红线大致水平横贯图像中部（r=64），绿线竖直、只在上半区（c=90，从顶边画到红线为止）。
> 控制台输出：l1 起点 (64,0)、l2 起点 (0,90)，两刀 A/B/C/D 指标全为 0，交点 (r=64, c=90)。
>
> **自查答案**：
> 1. 红线在中部有两层原因：① 谱字是上下结构，上部件在 20~58 行、下部件在 92~138 行，
>    中间 59~91 行是白色空隙，水滴走空隙所以切线在中部；② 初始滴点被约束在中部带
>    δ1∈(2/5·N, 3/5·N)（N=160 → r 只能在 64~96），四指标选中零熔断零偏移的 r=64。
> 2. 绿线没到底是因为第二刀只负责"左上 vs 右上"：l2 带 terminate_at=红线轨迹的终止条件，
>    踩到红线即停，交点 (64,90) 就是分割锚点，直接用来裁三张子图（论文 3.3.2）。

### 第 2 步：玩 —— 亲眼看"浅坑死锁"（约 20 分钟）

```bash
cd "D:/Study/算法学习/背景知识学习"
python 练习-mini_drop.py
```

把文件第 26 行 `r, c = 0, 1` 的列号依次改成 `2` 和 `3` 各跑一次。
**先猜再跑**：三种起点，两条会卡死在浅坑、一条能滑到底——先猜哪条能逃逸，再验证。

**自查**：卡死的两条轨迹末端有什么共同点？逃逸的那条绕过了坑的哪一边？

**我的记录**（2026-09-07 完成，第三次运行 c=3 时曾报 `IndexError: list index out of range`，
排查后发现是练习文件自身的边界 bug：`H, W = 6, 6` 但 `ink` 只有 5 行，已改为 `H, W = 5, 6`。
三组实际轨迹（`path` 末段）：

| 起点 | 我的猜测（卡死/逃逸） | 实际结果 | 轨迹末段 |
|---|---|---|---|
| c=1 | | 卡死 | `(2,2),(2,3),(2,2),(2,3),…` 在浅坑两格间往返 |
| c=2 | | 卡死 | `(1,3),(2,3),(2,2),(2,3),…` 同样在浅坑两格间往返 |
| c=3 | | 逃逸 | `(0,3),(0,4),(1,5),(2,5),(3,5),(4,5)`，落入最底行后在底行水平往返 |

**自查答案**：
1. 两条卡死路径的末端共同点：都在 `(2,2)` 与 `(2,3)` 之间**成对往复**——先往右再往左、
   再往右再往左，这就是传统 6 条规则的"水平往复循环"（浅坑死锁）的直观形态；
   规则 4/5（左右滑动）优先级相同且无记忆，所以永远出不去。
2. 逃逸的 c=3 绕过了坑的**右边**：正下/右下/左下全被挡时，水滴按规则 4 沿顶行右侧滑到
   (0,4)，再按规则 2 斜落到 (1,5)，之后沿最右列一路滑到底——绕开了浅坑的右壁。
3. 附赠的边界教训：底行水滴探测"正下方"时 `r+1` 越界——`H` 必须等于 `len(ink)`。
   练习文件里 `H, W = 5, 6` 是修复后的值。

### 第 3 步：改 —— 四种指标对比（约 20 分钟）

编辑 `../专利滴水算法实现/demo_segment.py` 第 55 行，把 `metric='D'` 依次改成
`'A'`、`'B'`、`'C'` 各跑一次，把控制台打印的初始滴点和指标值记进下表。

**自查**：四种指标选中的起点一样吗？如果一样，为什么论文实验里 D 仍是最优？
（提示：合成图太干净；想看难例分晓，可把 `run_real.py` 里的 `'D'` 改成 `'A'` 对比一次。）

**我的记录**（2026-09-08 ~ 09-10 完成，B/C 结果由辅导对话核对填入）：

| metric | l1 起点 | l2 起点 | l1 的 A/B/C/D | l2 的 A/B/C/D |
|---|---|---|---|---|
| A | (64, 0) | (0, 63) | 0/0/0/0 | 0/27/27/54 |
| B | (64, 0) | (0, 90) | 0/0/0/0 | 0/0/0/0 |
| C | (64, 0) | (0, 90) | 0/0/0/0 | 0/0/0/0 |
| D | (64, 0) | (0, 90) | 0/0/0/0 | 0/0/0/0 |

**A 行观察**（2026-09-08，完整解读见 [08-指标A实验解读.md](08-指标A实验解读.md)）：
l2 起点从 D 的 (0,90) 变成 (0,63)——指标 A 只数熔断，第 63 列"先滑 27 格绕行再落下"的路径
熔断也是 0，与直落并列第一；枚举规则"严格小于才更新"保留先到者 → 选 63。
代价是 B=27、C=27、D=54；两条路径终点相同 (64,90)，裁出的三张子图与 D 一致。

**自查答案**（四种指标选中的起点一样吗？为什么论文里 D 仍最优？）：
不完全一样——**只有 A 选 (0,63)，B/C/D 都选 (0,90)**。差别来自"并列最优"：合成图太干净，
多条候选在各指标上打平。A 只看熔断，绕行 27 格那条的熔断同样是 0（它是绕过去而不是切穿），
与直落并列，而枚举规则保留先到者 → 选中 63；B（始末偏移）与 C（累计偏移）能给绕行路径
打出 27 分的代价，因此把它淘汰，选中第一个直落列 90。D 把三者相加，干净图上与 B/C 同分，
真实难例上最稳——论文数据：无粘连集 A/D 并列 99.06%（B 96.26%、C 97.20%），
**粘连集 D 最高 95.53%**（A 掉到 91.96%），因为真实图上熔断与偏移常此消彼长，单指标顾此失彼。
（想亲手验证，可把 `run_real.py` 的 `'D'` 改成 `'A'` 跑一次对比。）

### 第 4 步：读 —— 专利图4~图12 对照代码（1~2 个晚上）

左手 `../文献/patent_pages/page_05.png` ~ `page_07.png`（图4~图12），
右手 `../专利滴水算法实现/improved_droplet.py` 的 `_q_decision` / `_step_patent`。

**推荐方法**：先跑 `python test_patent.py`——11 条 PASS 恰好就是图4~图12 的代码化。
逐条问自己：这条 PASS 对应哪个图？图里哪个格子是 `w`？代码哪个分支在处理它？`s` 怎么变？
能对上 7 条以上就算过关。

**我的记录**（2026-09-11 完成，11 条全部跑通并逐条对上专利图）：

> `python test_patent.py` 输出 11 条 PASS。逐条对照：
> 1→图4（Q=7 滴落）；2→图5(a)（Q=6 右下对角，s 保持）；3→图6(a)（Q=5 左下对角）；
> 4→图7(b)、5→图8(b)（Q=4/3 侧滑，s∈{0..3} 保持）；6/7/8→图9(a)(b)(c)（Q=2 浅坑的
> 飞溅 / 回流 / 熔断 三态）；9→图11（Q=0 全黑熔断）；10→图12（Q<0 仅对侧白回流）；
> 11→图10（Q=1，自补：`make({7})` + k=-1）。
>
> 一开始的卡点：把第 6 条当成了图10。后来明白 `make({6,7})` 在 k=1 时 o₆=+1、o₇=−1，
> n6 得 Q=2 胜出，所以第 6 条是图9；图10 要"只有 n7 开"才会出现 Q=1，于是单独补了第 11 条
> （用 k=-1，与图10 的左向一致；k=0 时 o₇ 也是 +1，结果相同——正好印证了 09 讲义 1.6 节
> "Q 的数值唯一编码方向"）。
>
> 仍可选的加餐（未做）：3 个 s 分支还没有独立断言——对角滴落 `s∈{1,3}`→s−1、`s=2`→0、
> 侧滑 `s∈{4,5}`→前跳归零；写法就是把第 2/4 条的 s 换个值、期望值相应调整。

### 第 5 步：语法补充 —— 带着问题查，不通读

- Python 官方中文教程：<https://docs.python.org/zh-cn/3/tutorial>
- numpy 入门：搜 "numpy quickstart"

---

## 卡住时怎么问（发给任何会话都适用）

1. 贴**报错全文**（最后十几行），别只贴一句"报错了"；
2. 说明做到清单**第几步**、改了**哪个文件哪一行**；
3. 如果是输出不符合预期，把**实际输出**和**你预期应该是什么**一起贴上。

---

## 当前进度（每次学完更新这一节）

- 2026-09-05：六课讲义读完，动手清单未开始。下一步：第 1 步 `demo_segment.py`。
- 2026-09-06：第 1 步完成，自查答案已填入第 1 步"我的记录"。下一步：第 2 步
  `练习-mini_drop.py` 浅坑实验（先猜再跑：三种起点先自己判断哪条能逃逸）。
- 2026-09-07：MATLAB 移植完成（`../MATLAB/`，本机 R2024b 跑通，与 Python 结果逐位对照），
  讲义新增 `07-MATLAB移植心得.md`（含动手清单，可与讲义 01~06 并行学习）。下一步仍是
  动手清单第 2 步；MATLAB 侧的实验可待 Python 清单做完后再做。
- 2026-09-07：动手清单第 2 步完成（浅坑死锁三组都跑通），记录与自查答案已填入；
  期间修复了练习文件的越界 bug（H=6→5）。**下一步：第 3 步 四种指标对比**——
  编辑 `../专利滴水算法实现/demo_segment.py` 第 55 行，把 `metric='D'` 依次改成
  `'A'`/`'B'`/`'C'` 各跑一次，把初始滴点与指标值记入第 3 步的表格。
- 2026-09-08：第 3 步的指标 A 完成——l2 起点变为 (0,63)、指标 0/27/27/54、交点仍 (64,90)，
  解读已写成 `08-指标A实验解读.md`；**下一步：先猜 B/C 的结果再跑**（猜：B、C 都应选 (0,90)），
  全部跑完把 `demo_segment.py` 第 65 行改回 `metric='D'`。
- 2026-09-10：第 3 步完成——B、C 实测均选 (0,90)，与猜测一致；A/B/C/D 四行与自查答案已填入，
  `demo_segment.py` 已改回 `metric='D'`（第 66 行调试用 `print(type(res), ...)` 可删）。
  讲义新增 `09-怎么读test_patent.md`（第 4 步导读：Q 是什么、断言骨架、十条图对照）。
  **下一步：第 4 步「读」**——跑 `python test_patent.py`，把 10 条 PASS 对照专利图4~图12 与
  `_q_decision`/`_step_patent`，目标对上 7 条以上；可选动手：仿照第 6 条补一条 `make({7})`
  （图10 Q=1）的断言，补上唯一的覆盖缺口。
- 2026-09-11：第 4 步「读」完成——`test_patent.py` 扩到 11 条断言（自补图10/Q=1，用 k=-1）
  并全部 PASS；11 条与专利图4~图12 的对照已填入第 4 步「我的记录」，动手清单四步
  （跑→玩→改→读）到此全部走完。讲义 `09-怎么读test_patent.md` 已同步为 11 条版本。
  剩余小尾巴：`demo_segment.py` 第 66 行的调试 `print` 已删除（2026-09-11 确认，脚本重跑结果不变）。**下一步（自选）**：
  ① 第 5 步语法补充（轻，随时可查）；② 可选加餐补 3 个 s 分支断言；③ 阶段三「真实粘连谱字
  切分改进」——需先设计方案（该话题要先读 `segment.py` 的初始滴点枚举，不要急着动代码）。
- 2026-09-11（同日）：讲义新增 `10-怎么读segment.py.md`（阶段三的前置阅读）——用"三步法"
  （看接口 → 追数据流 → 认模式/标危险点）拆解编排层，标出 3 处将来要改的地方（tie-break /
  切片边界 / None 兜底）、3 个动手实验（已给预测结果，跑完可自查）与 Python↔MATLAB 对照表。
  **下一步不变**：可选加餐补 3 个 s 分支断言，或开阶段三方案设计（先读讲义 10 + `segment.py`）。
- 2026-09-17：讲义新增 `11-如何读代码.md`——通用读码方法论（五步法：读说明书 → 找入口 →
  画零件地图 → 先注释后代码读核心 → 代入数字验证），以 `improved_droplet.py` 为完整实例，
  含语法速查表、手算对照演练（浅坑飞溅/回流两个例子）、阅读清单与自查题。
  **下一步不变**：可选加餐补 3 个 s 分支断言，或开阶段三「真实粘连谱字切分改进」方案设计
  （先读讲义 10 + `segment.py`，别急着动代码）。
- 2026-09-17（阶段三·第一轮）：真实谱字切分改进完成第一轮迭代。
  ① **身份复核（重要）**：`p49_img4` = **谱字A**——中途一次分析曾判定为"谱字B（粘连字）"，
  本次复核推翻（与图3-22(a) 位图 IoU 0.986 同一张嵌入图；论文第 39 页文字"以图3-22(a)所示
  谱字为例"；连通域 774 签名属 A 族）。教训：弱证据必须配合正确的出处假设，像素级对齐不可行。
  ② **改动**（`../专利滴水算法实现/segment.py`，滴水规则未动）：l2 选优默认改用**指标 B**
  （依据表3-1：粘连字只有 B 全对）；枚举改为**确定性**（`seed=None` 默认关闭随机取向；
  规则层与逐格校验照常保留 S102）。
  ③ **结果**：l2 漂移 9 → **0**、锚点 (92,46)（**92 是带 l₂ 终止条件时的实测行号**；
  此前文档里写的 96 是错的，2026-09-17 三次复核已更正）；但距论文真值（l2 终点 x_rel=0.524，
  图3-23 第一行四面板实测）仍差 **0.21 字宽**。（诊断结论见下一条，已推翻。）
  ④ 回归全绿：逐格校验 11/11、合成图 (64,90)/熔断 0 不变、严苛用例不变。全记录见
  `../专利滴水算法实现/验证报告.md` 的"阶段三更新"一节。
  注：讲义 08 第 3 步的 A 指标实验（l2 起点 63）是旧版行为——现在 `metric` 只管 l1、l2 由
  `metric2` 控制（默认 B）；想对比可显式传 `metric2='A'`。
  **下一步**：规则层研究（对照论文 §3.2 回流细节 / 沿斜笔画滑行方向），不要继续调指标层。
- 2026-09-17（阶段三·第二轮 · 规则层研究）：**推翻了上一轮的诊断结论**，真值口径已核实。
  ① **原诊断有误**：上一轮写"c0=77 每行右移一列右滑 34 列，论文实现从同一列基本直落"——
  实测 c0=77 的轨迹是「直落21行 → r=21 被迫侧滑(j=4,Q=4) → 右滑+对角到 c=103 →
  **笔直落 50 行** → 再侧滑 → (92,111)」，全程 `k≡0、s≡0`（随机取向与飞溅状态机未参与）。
  （**注意 (92,111) 是接了 l₂ 终止条件后的实测值**；不带终止条件是 (185,111)。
  早期写的 96 两者都不是，属误读，已更正。）
  ② **论文版规则走同一条路**：用论文 §3.2.1 的 5 邻点公式 `Q_i=max_{j=1..5}(z_j g_j)` 独立复现，
  从 c0=77 出发得 (81,110)，与本实现仅差 1 格 → "两代算法绕行细节不同"的假设**证伪**。
  ③ **δ₂ 带内枚举只有两个吸引子**（c0=42~48→终列44/27；c0=50~88→终列110/141），
  **不存在能到 0.524 的起点**。
  ④ **真值 0.524 已核实无误**：从 `../文献/HUNNUThesis.pdf` 第 47 页提取图3-23 四面板
  原始嵌入图（147×185，与 p49_img4 同尺寸），绿色像素分离量测 → **四面板 l₂ 终点全部
  = col 77，c/W = 0.5238**，口径就是 `c/W`。
  ⑤ **但论文 l₂ 是折线不是直线**：面板A `col57→col63(rows12-72)→右折→col77(rows84-124)`；
  面板B `col77→左折col63(rows55-65)→回col77`。真值线 **19%~24% 的行踩在墨迹上**
  → **论文 l₂ 在「大」字横笔处是横切穿越的**。
  ⑥ **当时猜的根因（已推翻，见下一轮）**：「大」的横笔是**斜**的（row21 墨迹到 col72、
  row25 左端退到 col59）；col=77 的墨迹段 = (22,27)(78,82)(129,134)，
  **row21 的 col77 是白、右邻也白 → 判 j=4 滑走**。穿第一段后 rows 28..77 有 50 行通畅，
  **需熔断总厚度仅 11px**。当时结论：差距在「遇单侧开放时是否选择熔断穿越」。
  **但这个猜测只查了"熔断"，没查"选优"——下一轮证明它不成立。**
  全记录见 `../专利滴水算法实现/验证报告.md` 的 4.5/4.6 节；新增诊断脚本
  （`diag_l2_trace.py` / `diag_gaps.py` / `diag_truth_measure.py` / `diag_topology.py` 等）。
  **下一步（已被下一轮取代）**：让 l2 选择穿越（熔断）。
- 2026-09-17（阶段三·第二轮 · 文件整理）：把项目从"脚本散落根目录"收成有目录结构的样子。
  ① **删除** 3 个废稿（`diag_fig325*.py`，三次失败尝试，量的是带页面文字的截图，结论错误）。
  ② **新建 `../专利滴水算法实现/diagnostics/`**，迁入 12 个诊断脚本，输出统一到
  `diagnostics/out/`（`diagnostics/out/thesis/` 放 PDF 提取图）；配 `diagnostics/README.md`
  （运行方式 + 按类别分组的脚本清单 + 已删脚本说明）。
  ③ **新建 `../专利滴水算法实现/CONTEXT.md`**——术语表（核心几何量 / 路径与方向 / 规则与状态 /
  评价标准 / 两代规则对照 / 输入与数据）。其中专门写了两条**歧义警示**：
  （i）"指标层"与"度量口径"是两件事（前者指 A/B/C/D 选优，后者指 c/W 真值定义）；
  （ii）`j=4 是"右侧"、j=5 是"左侧"`（相对当前前进方向 g 而言，不是图像坐标的左右）。
  这些正是本轮踩过坑的地方。
  ④ **脚本路径健壮化**：`demo_segment.py` / `run_real.py` / 全部 diagnostics 脚本的输出目录改为
  按 `__file__` 解析 → **在哪个目录运行都写到同一处**；迁移脚本统一注入
  `sys.path.insert(0, 上两级目录)`（放在第一个 `import os` 之后，避免 `os` 未定义）。
  ⑤ **验收（硬标准）**：迁移前后重跑 `diag_l2_trace.py` → 输出**逐字节相同**；
  `diag_gaps.py` → 仅第 42 行的输出路径行变化（预期内），其余逐字节相同。
  ⑥ **回归全绿**：`test_patent.py` **11/11 PASS**、`test.py` **400 组随机图全过** + 演示用例通过。
  ⑦ 删掉工作区根目录两份过期产物（`output/`、`output_real/`）；项目内
  `专利滴水算法实现/output_real/` 保留 5 张真实谱字交付图。
  **下一步不变**：让 l2 在「遇单侧开放但前方连续受阻」时选择穿越（熔断），见上一条。
  注：ADR（架构决策记录）**暂不写**——本轮只建术语表；若日后要留痕，候选条目是
  "专利 2024 版熔断优先级 → 论文图3-23 无法靠调参复现"。
- 2026-09-17（阶段三·第三轮 · **方向性结论：目标本身不成立**）：
  **把"复现图3-23 的 0.524"这个目标证伪了**，项目定位随之修正。
  ① **前两轮的方向都错了**。上一轮把差距归因于"遇单侧开放时是否熔断穿越"——
  这只查了**熔断**，没查**选优**。而"熔断"与"选优"是两件独立的事。
  ② **判别实验一**（`diagnostics/diag_l2_selection.py`）：枚举 **40 种组合**
  （δ₂ 带 5 种定义 × 指标 A/B/C/D × tie-break 严/宽），**没有一种能选出起点列 77**。
  原因是结构性的：δ₂ 带内所有起点只落到三个吸引子——`c0=8~27 → 终点 col 33`、
  `c0=28~49 → 终点 col 46`、`c0=50~110 → 终点 col 111`。起点 77 属最后一簇，
  终点是 **col 111**（不是真值 77）、横向偏移 34 格；而 42/46 那簇偏移仅 0~4 格。
  **77 在任何"越小越好"的指标下都排不进第一。**
  ③ **判别实验二**（`diag_l2_selection2.py`）：论文图3-23 四面板的三元组
  (起点列, 中间段列, 终点列) 是 `A(57,63,77) / B(77,63,77) / C(63,63,77) / D(63,63,77)`
  ——**起点各不相同，中间段与终点却完全一致**。
  ~~所以这四面板不可能是"同一算法跑四次"，更像同一条参考路径、四个不同起点的标注。~~
  ⚠️ **2026-09-18 更正**：四面板即 `(a)指标A (b)指标B (c)指标C (d)指标D`，**是同一谱字、
  同一算法的四次运行**；"起点不同而路径形状/终点相同"恰恰是**预期行为**——
  指标只改"选哪个起点"、不改规则与路径形状。**原推断作废。**
  而本实现在这张图上，各起点的中间段列取值集合里**根本没有 63**（只有 1~46 与 103~145 两簇）。
  ④ **结论**：差距**既不是指标选择、也不是绕行方向、也不是是否熔断穿越**，
  而是 —— **论文那条 l₂ 不是"改进滴水算法在 δ₂ 带内枚举选优"的产物**。
  若目标线本身不是本算法能产生的，**无论怎么改规则、怎么调参数都到不了**。
  ⑤ **这解释了文档里一直记着的"未解之谜"**（论文 l₂ 起点列 57/77/63 与 δ₂ 枚举结果不一致）：
  它**不是"风险"，而是该框架被证伪的反证**。此前把它当风险记着，
  导致后两轮都在"如何让 77 被选中/被走到"上打转。
  ⑥ **项目定位修正**：(i) 不再把"复现 0.524"当验收标准——本项目实现的是**专利 2024 版**，
  与论文图3-23 未必是同一个东西；(ii) 验证落在能自洽检验的地方（专利图4~图12 逐格 11/11、
  400 组随机图有界性、合成图零熔断、MATLAB 独立实现逐位对照）——这四项**已全绿**；
  (iii) **熔断改造失去依据，取消**。
  ⑦ ~~顺带更正几处文档错误：锚点行号 **96 → 92**（带 l₂ 终止条件时的实测值；
  96 既不是带终止条件的 92 也不是不带终止条件的 185），涉及 `验证报告.md` 多处 +
  本文件上一条~~ ⚠️ **2026-09-18 更正：这次"更正"方向搞反了。** `96/150/102` 才是对的，
  `92/149/98` 是**读图口径错误**的产物（`convert('L')<128` 把图内论文红线当墨迹）。
  详见本文件末尾 2026-09-18 条。
  `验证报告.md` 4.6 节的归因结论已改判，新增 4.7 节记录本轮的判别实验。
  新增两个只读诊断脚本 `diag_l2_selection.py` / `diag_l2_selection2.py`。
  **下一步**：(i) 停止追逐 0.524；(ii) 保留 l2 = B（有依据，但需注明"46 不是真值近似，
  只是自洽解"）；(iii) **可选低优先**——去论文正文（3.3.1 节）找图3-23 的生成说明，
  目前只核了**图与公式**，**没核过正文对实验设置的描述**，这是唯一还可能翻案的地方。

- 2026-09-18（阶段三·第六轮 · **两个根因级错误 + 结案**）：本轮不产生新结论，
  而是**推翻了本项目自己制造的两个错误**，并把上一轮遗留的"唯一还可能翻案的地方"关掉。
  ① **错误一（严重）：读图口径**。`文献/glyphs/p49_img4.png` **内烤着论文自己的红线标注**
  （247 个纯红像素 `(255,0,0)`，rows 94–184）。`convert('L') < 128` 会把纯红
  （`L = 0.299·255 ≈ 76 < 128`）**当成墨迹** ⇒ 水滴贴着论文红线走，l₁ 落到 **row 92**。
  正确口径 = **RGB 逐通道 `< 128`**（`run_real.py` 的 `load_ink`）。**唯一正确批**：
  l₁ 起点 **(95,0)** 长 **150**、l₂ 起点 (0,46) 长 **102**、锚点 **(96,46)**、
  l₁ 指标 **5/1/3/9**、l₂ 指标 12/0/10/22、三块 **96×46 / 96×101 / 89×147**。
  ⇒ 此前扩散到 5 个文档的 `92/149/98/5-0-2-7` **全部作废**；**"96 → 92"那次更正是改错了方向**。
  5 个诊断脚本统一改口径后重跑，定性结论不变。
  ② **错误二：δ 带误判**。曾以为"论文的 δ 是以中点为心的对称带、本实现取偏了"。
  **错**。论文 **§3.4.2** 用**五组范围对比实验**定值，采用 **δ₂=(2/7·M, 3/5·M)（实验三）**，
  **与本实现完全一致**；**实验五 (2/9·M, 7/9·M) 才是中点对称带，已被论文否决**。
  ⇒ "δ 带取错"从来不是变量，该理由作废；"未核实项 1（δ 半径）"**关闭**。
  ③ **新增：邻点集消融**（`diagnostics/diag_neighbor_ablation.py`）。三套邻点集
  （论文 §3.2.1 五邻点 / §3.2.2 六邻点 / 本实现七邻点）共用同一决策与终止条件，
  只变邻点集，在 δ₂ 带 47 个起点上对照：**六邻点 vs 七邻点 47/47 逐格一致**，
  **五邻点 vs 七邻点 47/47 全不同**。⇒ **"换成论文的规则"改变不了结果**，
  规则代次被排除。附带查清 **B 退化的机制层根因**：右簇内终点列恒为 col 111
  ⇒ B = 111−c₀ **严格单调** ⇒ **选中列恒等于起点范围的右边界**（"命中 77"因 77 是带右边界）
  ⇒ "让锚点=77"= **反向拟合**，不构成对算法的检验。（论文 §3.4.3 已报告该现象，
  本轮做的是**把它量化到机制层**，不宜说成自己的发现。）
  ④ **MATLAB 侧"待复核"结案**：实跑 `matlab -batch` 得
  `l1s=95,0 l2s=0,46 m1=5/1/3/9 m2=12/0/10/22 anchor=96,46 l1len=150 l2len=102`，
  与 Python **逐位一致**；同时 `test_patent` 11 项全 PASS、`test` 400/400 通过。
  ⇒ 两版**不存在终止逻辑差异**，"零代码差异、一处文字滞后"已改为"**零差异**"。
  ⑤ **回归全绿**：专利逐格 11/11、400 组随机、合成图交点 (64,90) 熔断 0、MATLAB 逐位对照。
  **算法行为零改动**，本轮只动文档与诊断脚本。
  ⑥ **新增汇报配图**：`../汇报/make_figs.py`（一键复跑）→ `../汇报/figs/` 三张 PNG
  （双向切分示意 + 真实谱字A 结果 / B 指标退化双面板 / 邻点集消融），
  已嵌入 `../汇报/项目汇报稿.md`，该稿同步精简。
