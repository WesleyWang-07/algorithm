# 项目长期记忆 —— 滴水算法（王子毅）

> 精炼版。日常流水见 `YYYY-MM-DD.md`；术语定义见 `专利滴水算法实现/CONTEXT.md`。

## 项目的真实目标（2026-09-17 已定案）

**实现专利 2024 版《基于水滴双向飞溅的滴水路径优化》，不是复现论文图3-23。**

- **不要**再以"l₂ 锚点列达到论文 0.524"为验收标准——经判别实验，那条线
  **不是**改进滴水算法在 δ₂ 带内枚举选优的产物（详见 `docs/adr/0001`）；
- 正确性由四项自洽检验承担：**专利逐格 11/11、400 组随机有界性、合成图零熔断、
  MATLAB 逐位对照**——已全绿；
- **不要**做"l2 熔断穿越改造"，前提已被证伪。

## 三条硬纪律

1. **判据用 ASCII 可判定的东西**：`PASS`/`FAIL` 计数、文件哈希、字节长度。
   Windows 控制台中文输出经管道会乱码且**不可逆**（PUA 填充 U+E000~U+F8FF），
   不要试图还原。管道给 Python 读 stdin 计数比落盘回读更稳。
2. **判断两套实现是否同步，读代码不看文档**：
   MATLAB 侧的 `metric2='B'` 早已同步，落后的是 README/CONTEXT 的文字。
3. **弱证据必须配合正确的出处假设**（`p49_img4` 身份那次误判的教训）；
   像素级对齐在跨图场景不可行。
4. **提交前先 `git status --porcelain` 分类**：本项目的暂存区极易积累 `AD`
   （已暂存新增 + 工作区已删）条目——移动文件时若不 `git add -A` 收尾，
   提交后等于"加了又删"，白提。**看到 `AD` 就先 `git reset` 再 `git add -A`。**
   `.workbuddy/`、`.vscode/` 不在 `.gitignore` 里（用户选择照原样提交），
   所以我生成的临时 `_*.txt` **必须自己清掉**，别被 `git add -A` 扫进去。

## 本机环境坑（Windows）

- **`Bash` 工具的 PATH 残缺**：`ls`/`cat`/`head`/`tail`/`wc`/`dirname` 全部
  `command not found`，但 `cd` + 绝对路径调 Python 可用。定式：
  `cd "路径" && "C:/Users/wzy/AppData/Local/Programs/Python/Python312/python.exe" -c "..."`
- **`timeout` 命令会撞上 Windows 的 `timeout.exe`**（报"无效语法"），
  改用 Bash 工具自身的 `timeout` 参数。
- **`git update-ref refs/remotes/origin/main` 会静默失败**：本机 `.git/refs/remotes/origin/`
  目录不存在时，它返回 0 但不落盘，导致 `git status` 一直显示 `[origin/main: gone]`。
  **修法：先用 Python `os.makedirs` 建目录，再写 `main` 文件（内容为哈希 + 换行）。**

## 关键数字（实测，勿沿用旧文档）

| 项 | 值 |
|---|---|
| 输入谱字 | `文献/glyphs/p49_img4.png`，147×185，= **谱字A**（非粘连字） |
| l1 | 起点 **r0=92**，轨迹 rows 92–93，路径长 **149** |
| l2 | 起点 (0,46)，路径长 **98**，锚点 **(92,46)**，`x_rel = 46/147 = 0.313` |
| l2 指标 | A/B/C/D = 12/0/10/22 |
| 论文图3-23 参考线 | 四面板终点均为 col 77，`c/W = 0.5238` |
| 环境检查 | `test_patent.py` **11/11 PASS**、`test.py` **400/400**、合成图交点 **(64,90)** |

**旧文档里的 `96`（锚点行号）、`150`/`102`（路径长）都是错的**，已更正为 92/149/98。
**`test_patent` 条数是 11 不是 10**（追补了图10 Q=1 用例），旧文档多处已更正。

## 「不追逐 0.524」的**完整**理由（2026-09-17 第六轮后定稿）

ADR 0001 的决定成立，但**经过两次加固**，现在的理由是三条叠起来的：

1. **δ₂ 带枚举选不出 77**（第三轮，40 种组合无一命中）；
2. **论文的 δ 是"以中点为心的对称带"**（正文 p45 原话），本实现用的 `(2/7, 3/5)`
   是偏左的 —— 旧枚举**漏试了对称带**；
3. **补试 16 档对称带后**（`diag_l2_band_center.py`）：唯一"命中"77 的是 `[71,77]`，
   因为 **77 恰是它的右边界**。逐列实测 B 在 c0=71..84 **严格单调**、
   **所有起点终点恒为 col 111** → **B 退化成"选带最右端"**，边界移到哪就选哪。
   **→ "让锚点=77"不构成对算法的检验，这正是被否决的"反向拟合"的形态。**

**反面教材（`图3-23 四面板`的读法）**：曾把四面板（`(a)指标A…(d)指标D`，同一谱字A）
误读成"四个不同起点的样本"，据此推断"不可能是同一算法跑四次"。
**实际是"指标只改起点、不改规则"，形状一致才是预期行为。** 已更正。

## 源头文献（别当截图删！）

`文献/源素材_作者微信说明_*.png` —— 作者（"圣缇香"）2026-06-13 微信记录，是**项目最初输入**：
红线=l₁(横)、绿线=l₂(竖) 的切分演示；以及作者说明图3-23 出自
**"专利算法的前身版本（改进之前）"**，并附 `HUNNUThesis.pdf`。
**来源是聊天记录、非正式文献，只作旁证。**（`图片材料/` 空目录已删，内容并入 `文献/`。）

## 术语歧义（重灾区，已在 CONTEXT.md 立规）

- **「指标层」≠「度量口径」**：前者指 A/B/C/D 选优，后者指 `c/W` 的定义。
  这两个问题曾被混成一个，导致浪费一轮研究。
- **`j=4` 是"右侧"、`j=5` 是"左侧"**：相对前进方向 g 而言，**不是图像坐标的左右**。
  在 l₁（g 向右）里 j=4 指的是**下方**。
- **「熔断」与「选优」是两件独立的事**：混成一件会在错误方向施工（第二轮教训）。
