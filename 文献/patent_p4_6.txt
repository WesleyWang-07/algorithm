===== patent_pages/page_04.png =====
这是一张文档页面的截图，看起来是一份专利申请书或技术规格说明书的一部分，页眉标注为“权利要求书”，页码为“3”。

以下是详细的内容描述：

**1. 顶部文本：**
页面顶部是一段说明性文字，内容为：“根据权利要求 3 所述的一种基于水滴双向飞溅的滴水路径优化生成方法，其特征在于：所述设计优先级从高到低为：滴落、飞溅、回流、熔断的滴水规则，具体的滴水规则的公式为：”

**2. 核心数学公式：**
紧接着上述文字，展示了一个复杂的分段函数公式，用于描述水滴位置 $(x_{i+1}, y_{i+1})$ 与当前状态 $(x_i, y_i, Q_i, s)$ 的关系。
*   **公式结构**：左侧是 $(x_{i+1}, y_{i+1}) = f(x_i, y_i, Q_i, s)$，右侧是一个巨大的左大括号，包含了约16行具体的条件分支。
*   **颜色特征**：在公式内部，有一列条件（主要涉及 $s$ 的值）使用了**红色字体**突出显示，例如 `s=0`，`s=s+1`，`s=s+2` 等。其余部分为黑色标准字体。
*   **公式内容示例**：
    *   第一行：$(x_i + 1, y_i) \quad \mathbf{s=0} \quad k=k \quad Q_i=7,0 \quad \forall s$
    *   中间部分包含多个坐标变化和对应的 $k$ 值（如 $k=k$ 或 $k=-k$）以及 $Q_i$ 的值（从7到0不等）。
    *   最后一行：$(x_i, y_i) \quad s=s+2 \quad k=-k \quad Q_i < 0 \quad \forall s$

**3. 中部文本（4.1节）：**
在公式下方，是编号为“4.1”的解释部分，详细定义了符号含义：
*   首先解释了像素点符号：“w”表示白色像素点，“b”表示黑色像素点，“*”表示黑色或者白色像素点，“#”表示所有像素点的并集为白色，即用“&”标出的像素点中至少有一个白色。
*   接着解释了状态变量 $s_i$ 和 $s_{i+1}$。
*   **紫色字体重点**：这一段中，关于 $s_i$ 取不同值时的具体含义被标记为**紫罗兰色（或淡紫色）**，非常醒目：
    *   当 $s_i = 0$ 时表示水滴等待飞溅；
    *   当 $s_i = 1$ 时表示水滴已经飞溅一个像素的高度；
    *   当 $s_i = 2$ 时表示水滴水平滚动反向一次；
    *   当 $s_i = 3$ 时表示已飞溅一个像素的高度并且水平滚动反向一次；
    *   当 $s_i = 4$ 时表示水平滚动反向两次；
    *   当 $s_i = 5$ 时表示已飞溅一个像素的高度并且水平滚动反向两次。

**4. 底部文本（4.2节）：**
页面最下方开始了一段新的说明，编号为“4.2”：
*   内容为：“$Q_i = 7$ 时，表示水滴竖直向下没有阻碍，对应不同的 $k$ 值，水滴都是向下滴落：”

**5. 布局与颜色总结：**
*   **布局**：典型的垂直排版，从上到下依次为主干说明、数学公式块、详细规则解释、下一规则开头。
*   **颜色**：主体文字为黑色宋体。公式中强调的条件用**红色**。对水滴状态 $s_i$ 的具体定义关键部分用了**紫色**。背景为纯白。

===== patent_pages/page_05.png =====
这张图片是一份技术文档或专利说明书的扫描件页面，主要内容是关于某种算法（看起来像图像处理或模式识别中的“滴落规则”）的状态转移说明。页面布局清晰，包含标题、公式、网格示意图和说明文字。以下是详细描述：

**1. 页面顶部**
*   **页眉**：正上方居中印有“权利要求书”字样。
*   **图4**：
    *   正文下方首先展示了“图4 $Q_i = 7$时的滴落规则”。
    *   这是一个3x3的网格图。
    *   网格上方标注公式：$S_{i+1}=0$。
    *   网格内容分布：
        *   第一行：左侧单元格为“b”，右侧单元格为“b”。
        *   第二行：左侧“b”，中间“*”（星号），右侧“b”。在中间的“*”下方有一个向下的箭头指向第三行的中间单元格。
        *   第三行：三个单元格均为“b”，其中中间单元格内也有一个小小的“b”。

**2. 第4.3节**
*   **标题**：“4.3 $Q_i = 6$时，说明此时$k=1$，对应的滴水规则如下：”
*   **图示部分（图5）**：
    *   下方标注“图5 $Q_i=6$时的滴落规则”。
    *   这一部分包含三组并排的3x3网格图，分别对应不同的条件：
        *   **(a) 组**：上方标注 $S_{i+1}=S_i$，下方标注 (a) $S_i=0, 4, 5$。网格内有星号“*”、“b”和“w”。有一个斜向右下的箭头指向右下角的“w”。
        *   **(b) 组**：上方标注 $S_{i+1}=S_i-1$，下方标注 (b) $S_i=1, 3$。结构与(a)类似，箭头同样指向右下角。
        *   **(c) 组**：上方标注 $S_{i+1}=0$，下方标注 (c) $S_i=2$。结构与前两者一致。

**3. 第4.4节**
*   **标题**：“4.4 $Q_i = 5$时，说明此时$k=-1$，对应的滴水规则如下：”
*   **图示部分（图6）**：
    *   下方标注“图6 $Q_i=5$时的滴落规则”。
    *   同样包含三组3x3网格图，但这次箭头方向改变为斜向左下：
        *   **(a) 组**：上方标注 $S_{i+1}=S_i$，下方标注 (a) $S_i=0, 4, 5$。箭头指向左下角的“w”。
        *   **(b) 组**：上方标注 $S_{i+1}=S_i-1$，下方标注 (b) $S_i=1, 3$。
        *   **第三组（标签印刷有误）**：上方标注 $S_{i+1}=0$，但下方的标签依然印的是“(b) $S_i=2$”（此处应为(c)）。箭头同样指向左下角。

**4. 第4.5节（页面底部）**
*   **标题**：“4.5 $Q_i = 4$时，说明此时$k=1$，且右下方的窗口为黑色像素点，对应的滴水规则如下：”
*   这段文字只给出了规则的条件描述，并未展示具体的图5或图6那样的网格示意图，文字在最后断开。

**5. 其他元素**
*   **符号含义**：图中频繁出现字母 $b$、$w$ 和星号 $*$。结合语境，“滴水规则”通常涉及二值图像（黑/白），推测 $b$ 代表黑色（black），$w$ 代表白色（white）。
*   **页码**：页面最下方居中有一个数字“4”。

总体来看，这是一份解释特定参数（$Q_i$ 取值为7, 6, 5, 4）下，系统如何根据相邻状态（$S_i$）进行状态转移（$S_{i+1}$）的技术图表。

===== patent_pages/page_06.png =====
这是一张包含技术文档内容的页面截图，页眉显示“权利要求书”，页脚显示页码“5”。页面主要展示了不同参数 $Q_i$ 取值下的规则示意图，通过网格图（类似元胞自动机的状态）来表示。

以下是图片内容的详细描述，按从上到下的顺序排列：

**1. 顶部图表（图7）**
虽然没有直接的章节编号，但这是页面的第一部分。
*   **左上角子图**：
    *   标题：$s_{i+1}=0$
    *   下方标注：(a) $s_i=4, 5$
    *   网格内容（3x3）：
        *   第一行：`*`，空格，`*`
        *   第二行：`*`，`*`（下方有向下箭头指向第三行的`b`），`w`
        *   第三行：`*`，`b`，`b`
*   **右上角子图**：
    *   标题：$s_{i+1}=s_i$
    *   下方标注：(b) $s_i=0, 1, 2, 3$
    *   网格内容（3x3）：
        *   第一行：`*`，空格，`*`
        *   第二行：`*`，`*`（右侧有向右箭头指向`w`），`w`
        *   第三行：`*`，`b`，`b`
*   **图注**：图7 $Q_i = 4$ 时的滴落规则

**2. 中间部分（4.6 章节与图8）**
*   **文本**：4.6 $Q_i = 3$ 时，对应的滴水规则如下：
*   **左上角子图**：
    *   标题：$s_{i+1}=0$
    *   下方标注：(a) $s_i=4, 5$
    *   网格内容（3x3）：
        *   第一行：`*`，空格，`*`
        *   第二行：`w`，`*`（下方有向下箭头指向第三行的`b`），`*`
        *   第三行：`b`，`b`，`*`
*   **右上角子图**：
    *   标题：$s_{i+1}=s_i$
    *   下方标注：(b) $s_i=0, 1, 2, 3$
    *   网格内容（3x3）：
        *   第一行：`*`，空格，`*`
        *   第二行：`w`（左侧有向左箭头从`*`指出），`*`，`*`
        *   第三行：`b`，`b`，`*`
*   **图注**：图8 $Q_i = 3$ 时的滴落规则

**3. 下部部分（4.7 章节与图9）**
*   **文本**：4.7 $Q_i = 2$ 时，对应的滴水规则如下：
*   **左侧子图**：
    *   标题：$s_{i+1}=s_i+1$
    *   下方标注：(a) $s_i=0, 2$
    *   网格内容（3x3）：
        *   第一行：`*`，空格，`w`
        *   第二行：`*`，`*`，`b` （注：此处有一个斜向上的箭头，从第二行左侧指向右上角的`w`）
        *   第三行：`*`，`b`，`b`
*   **中间子图**：
    *   标题：$s_{i+1}=s_i+2, k=-k$
    *   下方标注：(b) $s_i=1, 3$
    *   网格内容（3x3）：
        *   第一行：`*`，空格，`w`
        *   第二行：`*`，`*`（周围有顺时针旋转箭头），`b`
        *   第三行：`*`，`b`，`b`
*   **右侧子图**：
    *   标题：$s_{i+1}=0$
    *   下方标注：(c) $s_i=4, 5$
    *   网格内容（3x3）：
        *   第一行：`*`，空格，`w`
        *   第二行：`*`，`*`（下方有向下箭头指向第三行的`b`），`b`
        *   第三行：`*`，`b`，`b`
*   **图注**：图9 $Q_i = 2$ 时的滴落规则

**4. 底部文本（4.8 章节）**
*   **文本**：4.8 $Q_i = 1$ 时，对应的滴水规则如下：
*   此部分下方为空白，没有具体的图示。

**总结**：
页面主要由黑白线条构成的网格图和数学公式组成。网格中的字符包括 `*`（星号）、`w`、`b` 以及表示变化的箭头。这些图示似乎定义了某种逻辑或游戏（可能是元胞自动机）在不同状态 $s_i$ 和参数 $Q_i$ 下的转移规则。

